package dev.sgp.entity.npc;

import dev.sgp.entity.Entity;
import dev.sgp.gui.GamePanel;

public class NPC extends Entity {
    public NPC(GamePanel gamePanel) {
        super(gamePanel);
    }
    //placeholder for super NPC class
}
